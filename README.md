these are some files used to extract features from the dataset and train the model with the extracted data features from the dataset(DCASE 2020)


Main training code is the one with the name adam at the last.
